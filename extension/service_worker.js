// Service Worker for Team URL Saver
// Currently just a placeholder to satisfy MV3 requirements.
// Can be used for background tasks or context menus in the future.

chrome.runtime.onInstalled.addListener(() => {
    console.log('Team URL Saver Extension installed.');
});
